# (C# 코딩) 파일 컴페어

## 개요
- C# 프로그래밍 학습
- 1줄 소개 : 두 개의 폴더를 비교하고 최신 파일을 동기화해주는 파일 비교 프로그램
- 사용한 플랫폼:
    - C#, .NET Windows Forms, Visual Studio, GitHub

## 사용한 컨트롤
- SplitContainer: 양쪽 폴더의 리스트를 균형 있게 배치하기 위한 분할 컨테이너
- ListView: 파일명, 크기, 수정 날짜 등 파일 정보를 표 형태로 표시
- FolderBrowserDialog: 사용자로부터 작업할 폴더 경로를 입력받는 다이얼로그
- Button & TextBox: 경로 표시 및 복사 명령 실행을 위한 컨트롤

## 사용한 기술과 구현한 기능
- FileSystemInfo & DirectoryInfo: 파일과 하위 폴더의 속성 정보를 일관되게 취득
- Dictionary (자료구조): 양쪽 폴더의 파일명을 Key로 저장하여 빠른 비교 로직 구현
- 재귀적 알고리즘(Recursive): 하위 폴더 내부의 모든 파일과 폴더를 통째로 복사하는 기능
- 날짜 동기화 및 오차 제어: 복사 후 원본 날짜를 이식하고, 1초 미만의 시간 오차를 보정하여 비교 정확도 향상

---

## 실행 화면 (과제1)
- 코드의 실행 스크린샷과 구현 내용 설명

![실행화면](img/step1.png)

- 구현한 내용 (위 그림 참조)
    - FolderBrowserDialog를 이용한 작업 디렉터리 선택 기능
    - 선택된 폴더 내의 파일 및 하위 폴더 목록을 ListView에 출력
    - 파일 크기를 KB 단위로 변환하여 출력하는 포맷팅 기능
    - 폴더는 <DIR>로 표시하여 파일과 구분

## 실행 화면 (과제2)
- 코드의 실행 스크린샷과 구현 내용 설명

![실행화면](img/step2.png)

- 구현한 내용 (위 그림 참조)
    - 양쪽 폴더의 파일명과 수정 시간을 기준으로 상태 정의
    - 검정색: 양쪽 파일의 내용(시간)이 동일함
    - 빨간색: 원본이 상대방보다 더 최신인 상태 (New)
    - 회색: 원본이 상대방보다 오래된 상태 (Old)
    - 보라색: 해당 폴더에만 존재하는 단독 파일 (Unique)

## 실행 화면 (과제3)
- 코드의 실행 스크린샷과 구현 내용 설명

![실행화면](img/step3.png)

- 구현한 내용 (위 그림 참조)
    - 선택한 파일을 반대쪽 폴더로 복사하는 기능 구현
    - 대상 폴더에 파일이 존재할 경우 '덮어쓰기 확인' 팝업 출력
    - 특히 대상 파일이 원본보다 더 최신일 때만 경고 문구를 띄워 실수 방지
    - 원본과 대상의 전체 경로를 메시지 박스에 표시하여 가시성 확보

## 실행 화면 (과제4)
- 코드의 실행 스크린샷과 구현 내용 설명

![실행화면](img/step4.png)

- 구현한 내용 (위 그림 참조)
    - 하위 폴더 선택 시 내부 모든 구조를 유지하며 복사하는 재귀 로직 구현
    - 폴더 생성 시 수정 날짜가 현재 시간으로 변하는 OS 버그를 날짜 강제 이식으로 해결
    - 복사 완료 후 즉시 리스트뷰를 갱신하여 동기화 상태(검정색) 확인 가능
    - 1초 미만의 시간 오차를 허용하도록 비교 로직을 개선하여 비교 신뢰성 확보